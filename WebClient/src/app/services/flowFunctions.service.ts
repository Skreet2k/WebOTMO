import { Injectable } from '@angular/core';

import { Flow } from '../models/flow';

@Injectable()
export class FlowFunctionsService {
  getExperimentTime(flow: Array<number>) {
  }

  getFlowRate(flow: Array<number>, numFlow: number) {
  }

  getPacketsInterval(flow: Array<number>) {
  }
}
